# Spec Kit로 배우는 SDD(Spec-Driven Development)

**대상**: Claude Code(VS Code 확장) 사용 경험은 있음. 단, 명세서 작성, SDD 개념, GitHub Spec Kit, SQLite는 오늘 전부 처음 접함

**기술 스택**: Next.js(App Router) + TypeScript + Next.js API Routes + Prisma ORM + **SQLite(로컬 파일 데이터베이스)
형식**: 실습 포함, 배포는 다루지 않음(로컬 실행까지만)

**URL:** https://github.com/github/spec-kit

## 참고용 시간 배분 (조정 가능)

| 부  | 제목                                                             | 참고 시간 |
| --- | ---------------------------------------------------------------- | --------- |
| 0부 | 들어가며                                                         | 8분       |
| 1부 | 명세서와 SDD 제대로 이해하기                                     | 30분      |
| 2부 | [실습] 환경 준비: SQLite 이해하기 + Spec Kit 설치 + Next.js 시작 | 25분      |
| —   | 쉬는시간                                                         | 5분       |
| 3부 | [실습] Spec Kit 워크플로우로 기능 완성하기                       | 50분      |
| 4부 | 검증 & 결과 공유                                                 | 10분      |
| 5부 | 마무리                                                           | 3분       |

---

## 0부. 들어가며

### **1. 오늘 목표**

- 첫째, '명세서'라는 걸 왜 쓰는지, 어떤 종류가 있는지. 둘째, 그 명세서 작성을 도구화한 GitHub의 Spec Kit으로 AI에게 기능을 하나 통째로 맡겨서 완성하는 법.
- 오늘의 결과물: Next.js + SQLite로 만든 "할 일 API"가 명세 작성 → 계획 → 태스크 분해 → 구현의 전 과정을 거쳐 완성되는 것을 직접 경험하고, 데이터가 실제 파일(데이터베이스)에 저장되는 것을 눈으로 확인

### **2. AI에게 무작정 시키면 생기는 일**

- 일부러 애매한 지시를 Claude Code에 내려보자. 예: "할 일 관리 기능 좀 만들어줘."
- 예상되는 문제: 할 일에 어떤 정보가 들어가야 하는지(제목만? 마감일도?), 완료 처리는 어떻게 하는지, 데이터를 어디에 저장하는지 등 아무것도 정해주지 않았기 때문에 AI가 나름대로 추측해서 구현합니다. 결과물이 우리가 원한 것과 다를 확률이 높습니다.
- 알아서 잘 만들어줘'라는 지시는 사람 후배한테도 위험하지만 AI에게는 더 위험합니다. AI는 우리 머릿속 생각을 전혀 모르거든요. 오늘 하루는 이 문제를 '명세서'로 해결하는 법을 배웁니다.

### **3. 오늘 하루의 흐름 미리보기**

- 명세서가 뭔지, 왜 쓰는지, 어떤 종류가 있는지(1부) → 그 명세서 작성을 도구로 만든 Spec Kit 소개(1부) → 오늘 쓸 데이터베이스인 SQLite 이해하고 환경 준비(2부) → 실제로 Spec Kit의 6단계를 밟아 기능 완성(3부) → 결과 확인(4부)

---

## 1부. 명세서와 SDD 제대로 이해하기

### #4. 왜 명세서부터 쓰는가

명세서(Spec)란, "무엇을 만들 것인지"를 코드를 짜기 전에 글로 먼저 정리해둔 문서. AI가 등장하기 훨씬 전부터, 소프트웨어 개발에서는 항상 명세서를 먼저 썼습니다. 이유는 다음과 같습니다.

- **재현성**: 말로 대충 시키면 같은 요청도 사람마다, 상황마다 다르게 해석됩니다. 글로 적어두면 누가 봐도 같은 것을 만들게 됩니다.
- **커뮤니케이션 비용 감소**: 매번 처음부터 말로 설명하지 않아도, 문서 하나를 보여주면 됩니다.
- **리뷰 효율**: 결과물이 나왔을 때 "느낌이 다른가?"가 아니라 "명세서와 일치하는가?"로 검토할 수 있어 훨씬 빠르고 객관적입니다.
- **요구사항 누락 방지**: 머릿속으로만 생각할 때는 빠뜨리기 쉬운 조건(예: "제목이 비어있으면 어떻게 하지?")이, 글로 적다 보면 자연스럽게 드러납니다.
- **스코프 크립(scope creep) 방지**: "오늘 할 일"의 경계가 문서로 못박혀 있어야, 작업 도중 자꾸 범위가 늘어나는 것을 막을 수 있습니다.

AI와 협업할 때는 이 필요성이 훨씬 커집니다. AI 에이전트는 "이 팀에서는 당연한 것"에 관한 암묵적 지식이 하나도 없습니다. 명세서는 AI에게 "무엇이 완료 상태인지"를 알려주는 유일한 기준이 됩니다.

### #5. 명세서의 종류

명세서는 목적과 규모에 따라 여러 형태로 나뉩니다. 대표적으로 세 가지를 알아둡니다.

| 종류                                                          | 핵심 질문                            | 담는 내용                                                    | 담지 않는 내용                                        |
| ------------------------------------------------------------- | ------------------------------------ | ------------------------------------------------------------ | ----------------------------------------------------- |
| **PRD** (Product Requirements Document, 제품 요구사항 문서)   | 무엇을, 왜 만드는가                  | 대상 사용자, 해결하려는 문제, 사용자 시나리오, 성공 기준     | 기술 구현 방법(어떤 프레임워크, 어떤 DB 등)           |
| **TRD** (Technical Requirements Document, 기술 요구사항 문서) | 어떻게 만드는가                      | 아키텍처, 사용할 기술 스택, 입출력 형식(API 명세), 제약 조건 | "왜 이 기능이 필요한가" 같은 비즈니스 맥락            |
| **경량 통합 문서** (소규모 팀·개인 프로젝트용)                | 위 두 가지를 가볍게 합친 실전형 문서 | 목적(1~2문장) / 기능 목록 / 입출력 예시 / 제약, 정도로 압축  | 대규모 조직에서 필요한 승인 절차, 상세 리스크 분석 등 |

- 경량 통합 문서의 종류는 다양하며 널리 쓰인다. REQUIREMENTS.md가 대표적이다.
- PRD는 보통 "기획자·PM이 개발자에게 요구사항을 전달하는 문서", TRD는 "개발자가 그 요구사항을 어떻게 구현할지 정리한 문서"라고 생각하면 이해가 빠릅니다.
- 큰 회사의 정식 프로젝트는 PRD와 TRD를 따로, 두툼하게 씁니다. 하지만 개인이나 소규모 팀이 짧은 기능 하나를 만들 때는 이 둘을 가볍게 합친 "경량 통합 문서" 한 장이면 충분합니다.

경량 통합 문서 예시:

```
# 기능명: 완료 항목 삭제

## 목적
완료한 할 일이 목록에 계속 쌓이면 지저분하므로, 한 번에 지울 수 있어야 한다.

## 기능 목록
- 완료(done=true) 상태인 할 일만 골라서 삭제할 수 있다.
- 삭제된 개수를 사용자에게 알려준다.

## 입출력 예시
입력: 완료 항목 삭제 버튼 클릭
출력: "3개의 항목을 삭제했습니다" 메시지 표시

## 제약
- 완료되지 않은 항목은 삭제되지 않는다.
```

오늘 실습에서 쓸 Spec Kit이라는 도구도, 사실 이 표에 나온 개념을 그대로 자동화한 것입니다. 조금 뒤에 나올 `spec.md`는 PRD와 성격이 비슷하고(기술 얘기 없이 '무엇을'만), `plan.md`는 TRD와 성격이 비슷합니다(기술 스택과 구조를 정함). 이름만 다를 뿐, 오늘 배운 개념 그대로입니다.

### 6. SDD(Spec-Driven Development)란 무엇인가

핵심 원칙 한 문장으로: **"안정적인 '무엇(What)'과 유연한 '어떻게(How)'를 분리한다."**

- "무엇을 만들 것인가"(PRD에 해당)는 프로젝트 초반에 정해지면 잘 안 바뀝니다.
- "어떻게 만들 것인가"(TRD에 해당)는 기술 트렌드나 상황에 따라 자주 바뀔 수 있습니다.
- 이 둘을 문서 단계에서부터 분리해두면, "어떻게"가 바뀌어도 "무엇"을 다시 쓸 필요가 없습니다. AI에게 "어떻게" 부분만 다시 구현시키면 되니까, 반복 개발 비용이 훨씬 저렴해집니다.

코드를 먼저 짜는 게 아니라, **"실행 가능한 명세(spec)"** 를 프로젝트의 중심에 놓고, 그 명세를 기준으로 구현하고, 그 명세를 기준으로 완료 여부를 검증하는 개발 방식을 SDD라고 부릅니다.

비유: 건축에서 설계도 없이 바로 벽돌을 쌓지 않는 것과 같습니다. 설계도(명세)가 있어야 시공(구현)도, 준공 검사(검증)도 가능합니다.

### 7. Spec Kit이란 무엇인가

GitHub이 만든 오픈소스 SDD 툴킷입니다 (저장소 이름: `github/spec-kit`, MIT 라이선스로 누구나 무료로 사용 가능).

방금 배운 "PRD/TRD를 손으로, 매번 다른 형식으로 쓰는" 방식과 비교했을 때 Spec Kit이 나은 점:

1. **단계별로 강제된 산출물이 있습니다.** constitution(원칙) → spec(PRD에 해당) → plan(TRD에 해당) → tasks(작은 단위로) 라는 정해진 순서가 있어서, 매번 새로 형식을 고민할 필요가 없고 품질이 일정하게 유지됩니다.
2. **각 단계마다 "다음으로 넘어가도 되는지" 점검하는 절차가 내장되어 있습니다.** 예를 들어 clarify는 애매한 부분을 AI가 먼저 질문하게 만들고, checklist는 요구사항이 빠짐없이 채워졌는지 점검합니다.
3. **Claude Code 같은 AI 코딩 에이전트에서 슬래시 커맨드로 바로 실행됩니다.** Claude Code 채팅창에 `/speckit-`으로 시작하는 커맨드들이 새로 생깁니다.

필요한 준비물: Python 3.11 이상, `uv`(파이썬 도구를 설치·관리해주는 프로그램), Git, 그리고 Claude Code

### 8. Spec Kit 워크플로우 한눈에 보기

| 순서 | 커맨드                      | 이 단계에서 하는 일                                       | 앞서 배운 개념과의 관계             |
| ---- | --------------------------- | --------------------------------------------------------- | ----------------------------------- |
| 1    | `/speckit-constitution`     | 프로젝트의 원칙·개발 지침을 세움 (보통 프로젝트당 1회)    | 팀의 코딩 컨벤션·규칙을 정하는 문서 |
| 2    | `/speckit-specify`          | **기술 스택을 언급하지 않고** "무엇을 만들 것인가"를 정의 | PRD와 성격이 같음                   |
| 3    | `/speckit-clarify` (권장)   | plan 전에 애매한 부분을 AI가 먼저 질문해서 없앰           | "요구사항 누락 방지"를 자동화한 것  |
| 4    | `/speckit-plan`             | 기술 스택·아키텍처를 정해 "어떻게 만들 것인가" 계획       | TRD와 성격이 같음                   |
| 5    | `/speckit-tasks`            | 계획을 실행 가능한 작은 태스크 목록으로 쪼갬              | "작은 단위로 쪼개기"를 자동화한 것  |
| 6    | `/speckit-checklist` (선택) | 요구사항이 빠짐없이, 명확하게 채워졌는지 점검             | 제출 전 체크리스트 훑어보기         |
| 7    | `/speckit-implement`        | 태스크를 순서대로 실제로 실행해 기능을 완성               | 실제 구현 단계                      |

specify 단계(=PRD 작성 단계)에서는 절대로 "Next.js로", "SQLite로" 같은 기술 이야기를 하지 않습니다.

"무엇을(what)"과 "어떻게(how)"를 이 단계에서 섞어버리면, 방금 배운 SDD의 핵심 원칙이 무너집니다. 기술 스택 얘기는 오직 4단계(plan, TRD 작성 단계)에서만 등장합니다.

---

## 2부. [실습] 환경 준비: SQLite 이해하기 + Spec Kit 설치 + Next.js 시작

### 9. SQLite란 무엇인가 — 친절하게, 처음부터

- **한 문장 정의**: SQLite는 "서버 없이, 파일 하나로 통째로 작동하는 데이터베이스"입니다.
- **비유**: 흔히 아는 데이터베이스(MySQL, Postgres 등)는 "항상 켜져 있는 별도의 프로그램(서버)"에게 인터넷/네트워크를 통해 "이 데이터 좀 주세요"라고 요청하는 방식입니다. 은행 창구에 가서 직원에게 요청하는 것과 비슷합니다.
  SQLite는 다릅니다. 은행 창구가 따로 없고, **여러분이 직접 금고(파일)를 열어서 안에 있는 서류를 보는 것**과 같습니다. 데이터베이스 서버라는 "직원"이 아예 없고, 그냥 `dev.db`라는 파일 하나가 있을 뿐입니다. Prisma(오늘 쓸 ORM 라이브러리)가 이 파일을 직접 열고 읽고 씁니다.
- **왜 이게 오늘 실습에 딱 좋은가**:
  - 회원가입이 필요 없습니다. 계정도, 비밀번호도, 이메일 인증도 없습니다.
  - 인터넷이 끊겨도 실습이 가능합니다. 파일이 내 컴퓨터 안에 있으니까요.
  - 데이터베이스를 "리셋"하고 싶으면 `dev.db` 파일을 지우고 마이그레이션만 다시 실행하면 끝입니다.
  - 그런데도 우리가 배우는 개념(스키마 정의, 마이그레이션, ORM으로 데이터 읽고 쓰기)은 나중에 진짜 서비스용 데이터베이스(Postgres, MySQL 등)를 쓸 때도 거의 그대로 적용됩니다. **오늘 SQLite로 배우는 것이 나중에 "진짜" 데이터베이스로 그대로 옮겨갑니다.**

- **SQLite의 한계도 정직하게 알아두기**: 아주 많은 사용자가 동시에 접속해서 계속 쓰기 작업을 하는 실제 서비스(예: 배달 앱)에는 SQLite가 잘 맞지 않습니다. 파일 하나를 여러 곳에서 동시에 쓰려고 하면 병목이 생기기 때문입니다. "학습·프로토타입·개인 프로젝트에는 최고, 트래픽 많은 프로덕션 서비스에는 다른 선택지(Postgres 등)를 고려"라는 것이 일반적인 기준입니다.

### 10. 오늘 만들 것의 구조도

```
브라우저(Next.js 페이지)
   ↓
Next.js API Routes  (app/api/tasks/route.ts 등)
   ↓
Prisma Client  (타입스크립트 코드로 데이터베이스에 명령을 내리는 통역사 역할)
   ↓
SQLite 파일  (프로젝트 폴더 안의 dev.db 하나)
```

### 11. [실습] Spec Kit 설치

1. VS Code 내장 터미널 열기: 상단 메뉴 **Terminal → New Terminal** (또는 `` Ctrl+` `` / `` Cmd+` ``)
2. `uv`(파이썬 도구 설치·관리 프로그램) 설치 여부 확인:

   ```bash
   uv --version
   ```

   버전이 나오면 다음 단계로. 안 나오면 아래 명령으로 설치:
   - Mac / Linux:

     ```bash
     curl -LsSf https://astral.sh/uv/install.sh | sh
     ```

   - Windows (PowerShell):

     ```powershell
     powershell -ExecutionPolicy ByPass -c "irm https://astral.sh/uv/install.ps1 | iex"
     ```

   - 설치 직후에는 터미널을 완전히 닫았다가 새로 열어야 `uv` 명령을 인식하는 경우가 있습니다.

3. Specify CLI(Spec Kit의 명령줄 도구) 설치:

   ```bash
   uv tool install specify-cli --from git+https://github.com/github/spec-kit.git
   ```

4. 잘 설치됐는지 확인:

   ```bash
   specify --version
   ```
* 이후, 컴퓨터에 재설치 불필요. 다른 프로젝트에서 사용하고 싶을 때는, 다음 12. [실습]과 같이 바로 Spec Kit 초기화하여 사용하면 된다.

### 12. [실습] Next.js 프로젝트 생성 + Spec Kit 초기화

1. 원하는 위치에서 터미널에 아래 명령을 입력해 새 Next.js 프로젝트를 생성합니다.

```bash
   npx create-next-app@latest mini-todo-sqlite --typescript --app --eslint
```

2. 방금 만든 폴더를 VS Code에서 열기: **File → Open Folder** → `mini-todo-sqlite` 폴더 선택
3. VS Code 터미널에서, 지금 열려 있는 이 프로젝트 폴더 안에 Spec Kit을 초기화합니다. 새 폴더를 따로 만들지 않고 **현재 폴더에** 설치하는 옵션(`--here`)을 씁니다.

```bash
   specify init --here --integration claude
```

4. 결과 확인: VS Code 왼쪽 탐색기에 `.specify/` 폴더와 `.claude/commands/speckit.*.md` 같은 파일들이 새로 생긴 것을 확인합니다.
5. Claude Code 패널을 열고 `/`를 입력해서 `speckit.`으로 시작하는 커맨드들이 실제로 목록에 뜨는지 확인합니다.

_(쉬는시간)_

---

## 3부. [실습] Spec Kit 워크플로우로 기능 완성하기

오늘의 실습 시나리오: **"할 일을 추가/조회/완료 처리/삭제할 수 있는 API + 화면"** 을, 데이터가 사라지지 않고 SQLite 파일에 실제로 저장되도록 만들기

Claude Code는 (Auto가 아닌) Manual 모드(Mode)로 실행

### #13. [실습] constitution — 프로젝트 원칙 세우기

```
/speckit-constitution 이 프로젝트는 Next.js(App Router) + TypeScript로 작성한다. API 응답은 항상 JSON 형태로 통일한다. 타입은 any를 쓰지 않는다.
```

- 생성/갱신된 `.specify/memory/constitution.md`를 함께 읽어봅니다.
- constitution은 프로젝트 전체에 적용되는 '헌법' 같은 것입니다. 보통 프로젝트를 시작할 때 한 번만 정하고, 이후에는 계속 재사용합니다.

### 14. [실습] specify — 무엇을 만들지 정의 (PRD에 해당)

```
/speckit-specify 사용자가 할 일을 추가하고, 목록을 확인하고,
완료 여부를 토글하고, 삭제할 수 있는 기능이 필요하다.
할 일에는 제목(필수)과 완료 여부가 있다.
```

- 생성된 `spec.md`를 다 같이 열어서, 1부에서 배운 PRD의 성격(무엇을, 왜 / 기술 언급 없음)이 그대로 담겨 있는지 확인합니다.
- 실습 미션: 짝과 함께 spec.md를 읽고, "이건 우리가 의도한 것과 좀 다른데?" 싶은 부분을 하나씩 찾아 표시해보기.

### 15. [실습] clarify — 애매한 부분 없애기

```
/speckit-clarify
```

- Claude가 spec.md를 다시 읽고 스스로 애매하다고 판단한 부분에 대해 역으로 질문을 던집니다(예: "완료된 할 일도 삭제할 수 있어야 하나요?", "제목의 최대 글자 수는?"). 하나씩 답변합니다.
- 읽고 답하는 것이 귀찮지만, 고급 개발자가 되기 위해, 모두 꼼꼼히 읽고 생각하고 답하는 습관을 가지는 것이 중요합니다.
- 이 단계가 바로, 1부에서 배운 '요구사항 누락 방지'를 AI가 대신 해주는 것입니다.

### 16. [실습] plan — 기술 스택과 아키텍처 정하기 (TRD에 해당)

```
/speckit-plan Next.js App Router의 API Routes(app/api/tasks/route.ts 등)로 REST 엔드포인트를 만든다. ORM은 Prisma를 쓰고, 데이터베이스는 로컬 SQLite 파일(dev.db)을 사용한다. 별도의 서버 설정이나 외부 서비스 가입은 필요 없다.
```

생성된 아티팩트(참고):
- plan.md — Technical Context(Next.js App Router + Prisma + SQLite), Constitution Check(3원칙 모두 PASS), 프로젝트 구조
- research.md — Phase 0: ORM/DB, 테스트 프레임워크(Vitest), JSON 응답 스키마, 토글 API 방식(명시적 PATCH), 정렬(최신순), 삭제 확인 처리 위치, id 타입 등 7개 결정 사항
- data-model.md — Todo 엔티티(id, title, completed, createdAt), 검증 규칙, 상태 전이, Prisma 스키마 초안
- contracts/tasks-api.md — GET/POST /api/tasks, PATCH/DELETE /api/tasks/{id} 요청/응답 JSON 계약
- quickstart.md — curl + Vitest 기반 시나리오별 수동/자동 검증 가이드



생성된 `plan.md`를 함께 열어봅니다. 보통 아래와 같은 내용이 담깁니다.

- 엔드포인트 설계: `GET /api/tasks`, `POST /api/tasks`, `PATCH /api/tasks/[id]`, `DELETE /api/tasks/[id]`
- Prisma 스키마에 들어갈 `Task` 모델(제목, 완료 여부, 생성 시각 등)
- SQLite 연결 방식: `.env`에 `DATABASE_URL="file:./dev.db"` 한 줄



### 17. [실습] tasks — 작은 단위로 쪼개기

```
/speckit-tasks
```

- 몇 개의 Phase로 나누어 구현하려 하나요? 
- 생성된 `tasks.md`를 함께 읽으며, 계획이 작은 단위로 쪼개졌는지 확인합니다. (예: Prisma 설치 → 스키마 정의 → 마이그레이션 → GET 구현 → POST 구현 → PATCH 구현 → DELETE 구현 → 프론트 연결).

- (여유가 있다면) `/speckit.checklist`로 요구사항이 tasks에 빠짐없이 반영됐는지 한 번 더 점검합니다.

### 18. [실습] implement — 실제로 구현하기

```
/speckit-implement
```

- Claude Code가 tasks.md를 순서대로 실행합니다. 중간중간 뜨는 권한 요청(`npm install`, `npx prisma migrate dev` 등)에 **Allow**를 눌러줍니다. `npx prisma migrate dev`가 실행되는 순간, 프로젝트 폴더 안에 `dev.db` 파일이 실제로 생깁니다.
- 만들어지는 주요 파일:
  - `prisma/schema.prisma`: 어떤 테이블(모델)이 있는지 정의하는 설계도
  - `.env`: `DATABASE_URL="file:./dev.db"` 딱 한 줄
  - `lib/prisma.ts`: Prisma Client를 만들어 다른 코드에서 불러다 쓰게 해주는 파일
  - `app/api/tasks/route.ts`: 실제로 요청을 받아 Prisma Client로 데이터베이스를 읽고 쓰는 코드


---

## 4부. 검증 & 결과 공유

### 19. 결과 확인하기

1. 터미널에서 개발 서버 실행: `npm run dev`
2. 브라우저에서 `http://localhost:3000` 접속 → 할 일 추가/완료 체크/삭제 테스트
3. **오늘 실습의 하이라이트**: 방금 추가한 데이터가 정말 SQLite 파일에 저장됐는지 확인
   - **방법 1**: 새 터미널에서 `npx prisma studio` 실행 → 브라우저에 표 형태로 실제 데이터 확인
   - **방법 2**: VS Code 탐색기에서 `dev.db` 파일 용량이 변하는 것을 관찰

4. 0부에서 본 애매한 지시의 결과와 비교해봅니다. 오늘은 명세서를 단계별로 밟아왔기 때문에, 원하는 그대로 만들어졌고, 게다가 데이터가 실제로 남아있습니다.

### 20. 토의 및 공유

- SDD가 좀 이해가 되나요?
- Spec Kit은 어떤 도구인가요?

---

## 5부. 마무리

### 21. 오늘의 체크리스트 정리

- 명세서란 무엇이고 왜 쓰는지, PRD/TRD/경량 통합 문서의 차이
- SDD("무엇"과 "어떻게"의 분리)
- Spec Kit 6단계 워크플로우(constitution/specify/clarify/plan/tasks/implement)
- SQLite가 무엇이고 왜 서버가 필요 없는지, Prisma로 SQLite에 저장하고 Prisma Studio로 확인하는 법

### 22. 다음 시간 예고

- Spec-Kit 대신, Matt Pocock' Skill을 이용해 SDD 구현
- URL: https://www.aihero.dev/skills
- Youtube: https://www.youtube.com/@mattpocockuk

### 23. 과제
1. 할 일 카드에 중요도(priority)를 넣으시오. 우선순위는 3단계(High, Medium, Low)로 하시오. Plan 모드에서 Claude Code에게 이 기능을 어떻게 추가할지 물어보고 구현하시오.
2. 완성된 프로젝트를 여러분의 Github Repository에 넣고, URL을 제출하시오.
3. 동영상 시청(17:16): https://www.youtube.com/watch?v=M6mYodf0dJM